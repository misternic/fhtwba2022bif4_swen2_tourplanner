<Window x:Class="TourPlanner.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:TourPlanner.ViewModels" xmlns:Views="clr-namespace:TourPlanner.Views"
        mc:Ignorable="d"
        Title="Tour-Planner" Height="450" Width="800">
    <Window.DataContext>
        <local:MainViewModel />
    </Window.DataContext>
    <DockPanel>
        <Menu DockPanel.Dock="Top">
            <Menu.ItemsPanel>
                <ItemsPanelTemplate>
                    <DockPanel HorizontalAlignment="Stretch"/>
                </ItemsPanelTemplate>
            </Menu.ItemsPanel>
            <!-- underline in Header is key shortcut -->
            <MenuItem Header="_File">
                <MenuItem Header="_New" />
                <MenuItem Header="_Open" />
                <MenuItem Header="_Save" />
                <Separator />
                <MenuItem Header="_Exit" />
            </MenuItem>
            <MenuItem Header="_Edit">
            </MenuItem>
            <MenuItem Header="_Options">
            </MenuItem>
            <MenuItem Header="_Help" HorizontalAlignment="Right">
                <MenuItem Header="_About"/>
            </MenuItem>
        </Menu>
        <Views:SearchBar DockPanel.Dock="Top" />
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="200" MinWidth="100"></ColumnDefinition>
                <ColumnDefinition Width="5"></ColumnDefinition>
                <ColumnDefinition Width="200*" MinWidth="200"></ColumnDefinition>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="200"></RowDefinition>
                <RowDefinition Height="5"></RowDefinition>
                <RowDefinition Height="200*"></RowDefinition>
            </Grid.RowDefinitions>

            <DockPanel Grid.RowSpan="3">
                <DockPanel DockPanel.Dock="Top" Margin="1,1,1,1">
                    <StackPanel DockPanel.Dock="Right" Orientation="Horizontal">
                        <Image Source="/images/plus-box.png" Width="18" Height="18" />
                        <Image Source="/images/close-box.png" Width="18" Height="18" />
                        <Image Source="/images/pencil.png" Width="18" Height="18" />
                    </StackPanel>
                    <Label Content="Tours" FontWeight="Bold" />
                </DockPanel>
                <ListBox x:Name="tourItems"
                         ItemsSource="{Binding Tours}"
                         DisplayMemberPath="Name" />
            </DockPanel>
            <GridSplitter Grid.Column="1" Grid.Row="0" Grid.RowSpan="3" HorizontalAlignment="Stretch" VerticalAlignment="Stretch"></GridSplitter>
            <Label Grid.Row="0" Grid.Column="2" Content="Tours" />
            <GridSplitter Grid.Column="2" Grid.Row="1" HorizontalAlignment="Stretch" VerticalAlignment="Stretch"></GridSplitter>
            <Label Grid.Row="2" Grid.Column="2" Content="Logs" />
        </Grid>
    </DockPanel>
</Window>
